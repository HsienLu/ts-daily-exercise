"use strict";
/*

TypeScript 中，函式陳述式的主要特點是什麼？
B. 必須要有函式名稱
在 TypeScript 中，函式參數的型別註釋主要作用是什麼？
B. 定義函式的參數和回傳值的型別
以下哪個選項正確定義了一個 TypeScript 函式，該函式接受兩個 number 類型的參數並回傳一個 number？
B. const add = (x: number, y: number) => x + y;
如果一個 TypeScript 函式不回傳任何值，它的回傳型別應該是什麼？
C. void

*/
function multiply(x, y) {
    return x * y;
}
const concatString = (x, y) => x + y;
function isGreaterThanTen(x) {
    return x > 10;
}
